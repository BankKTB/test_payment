import { DetailPaymentBlockPipe } from './detail-payment-block.pipe';

describe('DetailPaymentBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailPaymentBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
