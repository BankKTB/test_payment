import { ReasonReversePipe } from './reason-reverse.pipe';

describe('ReasonReversePipe', () => {
  it('create an instance', () => {
    const pipe = new ReasonReversePipe();
    expect(pipe).toBeTruthy();
  });
});
