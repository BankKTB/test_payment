import { DatePatternPipe } from './date-pattern.pipe';

describe('DatePatternPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePatternPipe();
    expect(pipe).toBeTruthy();
  });
});
