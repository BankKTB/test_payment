import { TimePatternPipe } from './time-pattern.pipe';

describe('TimePatternPipe', () => {
  it('create an instance', () => {
    const pipe = new TimePatternPipe();
    expect(pipe).toBeTruthy();
  });
});
